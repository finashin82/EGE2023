list('список') # Создание списка из слова список // ['с', 'п', 'и', 'с', 'о', 'к']

s = []  # Пустой список

Создание списков:

c = [c * 3 for c in 'list'] // ['lll', 'iii', 'sss', 'ttt']

c = [c * 3 for c in 'list' if c != 'i'] // ['lll', 'sss', 'ttt']

c = [c + d for c in 'list' if c != 'i' for d in 'spam' if d != 'a']
// ['ls', 'lp', 'lm', 'ss', 'sp', 'sm', 'ts', 'tp', 'tm']

list.append(x) # Добавляет элемент в конец списка

list.extend(L) # Расширяет список list, добавляя в конец все элементы списка L

list.insert(i, x) # Вставляет на i-ый элемент значение x

list.count(x) # Возвращает количество элементов со значением x

list.sort([key=функция]) # Сортирует список на основе функции

str.join(iterable) # str - строка-разделитель
                   # iterable - итерируемый объект с элементами в виде строк
# Возвращает строку, которая является конкатенацией

